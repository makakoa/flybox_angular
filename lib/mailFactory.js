// Should cover to member being user/not, and whether to send link/text
'use strict';

//var User = require('../models/user');

module.exports = function(smtpOptions, body) {
  if (smtpOptions && body);
};
